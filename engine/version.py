"""Single source of truth for the Beacon engine version.

The updater compares this against the `version` field in the published
release manifest (version.json). Bump on every release.
"""

__version__ = "0.1.18"
